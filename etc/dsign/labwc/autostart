#!/bin/sh
# Cursor hide + idle re-hide are handled by systemd ExecStartPost:
#   /usr/local/bin/dsign-labwc-autostart (dsign-compositor.service)
# Do not start swayidle here — labwc -C also runs this file and would duplicate swayidle.
