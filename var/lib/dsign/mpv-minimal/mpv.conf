# Minimal MPV config for dsign-mpv.service (--config-dir=/etc/dsign/mpv-minimal).
# yt-dlp must be on PATH for the dsign user, or add:
#   script-opts=ytdl_hook-ytdl_path=/full/path/to/yt-dlp
# (see mpv ytdl_hook documentation).
script-opts=ytdl_hook-ytdl_path=/usr/bin/yt-dlp
